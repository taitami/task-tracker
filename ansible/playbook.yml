---
- name: Setup Common configurations on all nodes
  hosts: all
  become: yes
  roles:
    - common

- name: Setup Database node
  hosts: db
  become: yes
  roles:
    - db

- name: Setup Worker node (Web App & Nginx)
  hosts: workers
  become: yes
  roles:
    - webapp
    - nginx